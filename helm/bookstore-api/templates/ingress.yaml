apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bookstore-api
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: {{ .Values.ingress.className }}
  rules:
    - host: bookstore.local     # Replace this with your real domain or add to /etc/hosts for local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bookstore-api
                port:
                  number: 8080
